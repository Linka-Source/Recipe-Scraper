<!DOCTYPE html>
<html lang="en">

<head>
    <title>Passport Authentication</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="https://unpkg.com/tailwindcss@^2/dist/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.2/css/bulma.min.css">
    <link href="/css/style.css" rel="stylesheet">
</head>

<body>
    <header>
        <nav class="navbar" role="navigation" aria-label="main navigation">
            <div class="navbar-brand">
                <a class="navbar-item" href="https://bulma.io"></a>
                    <img src="https://www.nicepng.com/png/detail/205-2056722_heat-cooking-logo-png.png" width="112" height="28">
                </a>

                <a role="button" class="navbar-burger" aria-label="menu" aria-expanded="false"
                    data-target="navbarBasicExample">
                    <span aria-hidden="true"></span>
                    <span aria-hidden="true"></span>
                    <span aria-hidden="true"></span>
                </a>
            </div>
            <div id="navbarBasicExample" class="navbar-menu">
                <div class="navbar-start">
                    <a class="navbar-item btn" href="/">Home</a>
                    {{#if loggedIn}}<a class="navbar-item" href="/dashboard">Dashboard</a>{{/if}}
                </div>
                <div class="navbar-end">
                <div class="navbar-item">
                <div class="buttons">
                    {{#unless loggedIn}}<a class="btn" href="/dashboard"> <strong>Login</strong></a> / <a class="btn" href="/Dashboard">Signup</a>
                    {{/unless}}
                    {{#if loggedIn}}<a class="btn" href="/logout" id="logout">Logout</a>{{/if}}
                </div>
                </div>
                </div>
            </div>
        </nav>
    </header>
    {{{body}}}
</body>

</html>
